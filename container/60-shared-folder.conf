# mybox drop-in: shared-folder — host /mnt/shared at /mnt/shared.
#
# Parallel to incus `shared-folder` profile. Put symlinks inside the
# host /mnt/shared (repo checkouts, downloads, media, /mnt/data) —
# keeps the drop-in portable across hosts.

[Container]
Volume=/mnt/shared:/mnt/shared:idmap
