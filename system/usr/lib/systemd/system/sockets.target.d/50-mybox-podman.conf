# Auto-start the system podman socket activator. Upholds= (vs static
# .wants/) re-evaluates on daemon-reload, so the socket starts even
# after a config refresh. The service itself is socket-activated — no
# need to uphold it.
[Unit]
Upholds=podman.socket
