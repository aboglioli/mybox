# Under podman, netavark configures eth0 host-side BEFORE PID 1 and
# networkd manages no link — wait-online would block
# network-online.target for its full timeout (~75 s boot stall)
# waiting for a link that never comes under its watch. Skip it.
[Unit]
ConditionVirtualization=!podman
