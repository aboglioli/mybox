# User default.target drop-in — wireplumber session manager for
# pipewire. Without it the pipewire daemon runs but no policy
# engine assigns devices/streams to nodes → no audio.
#
# Same pattern as myosi's desktop profile.
[Unit]
Upholds=wireplumber.service
