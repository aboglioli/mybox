# User-scope sockets.target drop-in — auto-start rootless podman
# socket: rootless API reachable at
# $XDG_RUNTIME_DIR/podman/podman.sock without explicit
# `systemctl --user start podman.socket`.
[Unit]
Upholds=podman.socket
