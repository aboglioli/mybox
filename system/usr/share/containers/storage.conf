# Storage config for NESTED podman (the podman running INSIDE mybox).
# Vendor tier of the containers-storage.conf(5) search path; user
# override via /etc/containers/storage.conf still works.
#
# rootless_storage_path keeps rootless storage OUT of the user's home:
# an encrypted fixed-size home shouldn't hold images, and an idmapped
# home mount only maps the user's stable UID — podman's subuid pool
# (100000+) has no translation there and lchown throws EOVERFLOW.
# The per-user tree /var/lib/containers/users is created 1777 by
# tmpfiles.d/mybox.conf so each user owns its subdir.
#
# NO graphroot/runroot on purpose (podman 5.8+): an explicit graphroot
# OVERRIDES rootless_storage_path, and an explicit runroot sends
# rootless to root-owned /run/containers/storage → EPERM. Omitting
# both lets podman pick correct per-scope defaults.

[storage]
driver = "overlay"
rootless_storage_path = "/var/lib/containers/users/$UID"

[storage.options.pull_options]
# Pull only modified blocks from zstd:chunked images; full-pull
# fallback when unsupported.
enable_partial_images = "true"

# No metacopy=on: the kernel disables metacopy on rootless userxattr
# overlay mounts (privilege-escalation guard), so containers/storage
# would strip it and log "Ignoring global metacopy option" on every
# command.
[storage.options.overlay]
mountopt = "nodev"
